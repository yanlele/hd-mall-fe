const ApiPrefix = '/api/admin';

// 获取用户信息接口
export const getUserInfoApiUrl = `${ApiPrefix}/user/info`;

// 登录接口
export const loginApiUrl = `${ApiPrefix}/auth/login`;

// 注销接口
export const logoutApiUrl = `${ApiPrefix}/auth/logout`;
